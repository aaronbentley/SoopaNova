/** @jsx jsx */
import { Heading } from '@theme-ui/components'
import { jsx } from 'theme-ui'

// export const Clip = ({ location: { state } }) => {
const Clip = ({ location: { state } }) => {
    // console.log(state)
    return <Heading>Clip</Heading>
}

export default Clip
